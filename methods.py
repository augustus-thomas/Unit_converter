class Methods:

    #Simply applies the appropriate conversion rate
    def conversion(in_val, out_val, c_rate):
        self.out_val = self.in_val * self.c_rate
    
    #Reads input from the text box currently used. Refreshes on keystroke (real-time)
    def reader():
        in_val

    #Writes converted values to the other text boxes
    def writer():
        print(self.conversion)
